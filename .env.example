# Copy this to .env and fill in your values. Never commit .env.

TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here
GEMINI_API_KEY=your-gemini-api-key-here

# Your personal Telegram chat ID. The bot will ignore everyone else.
# Find it by messaging @userinfobot on Telegram.
ALLOWED_CHAT_ID=123456789

# Optional overrides (defaults shown)
# GEMINI_FAST_MODEL=gemini-2.5-flash-lite-preview-06-17
# GEMINI_STRONG_MODEL=gemini-2.5-pro
# DB_PATH=data/assistant.db

# --- Gmail (optional) ---
# Read-only unread-email tool. Leave unset to run without it.
# 1. Google Cloud Console → enable Gmail API → create OAuth "Desktop app" client.
# 2. Download the client-secret JSON to the path below.
# 3. Authorize once:  python -m tools.gmail
# GMAIL_CREDENTIALS_PATH=data/gmail_credentials.json
# GMAIL_TOKEN_PATH=data/gmail_token.json
